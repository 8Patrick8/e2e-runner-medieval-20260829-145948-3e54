// Stub — the background ticket implements parallax scrolling.
export function updateBackground(state, dt) {}

// Stub — the background ticket implements parallax rendering.
export function drawBackground(ctx, state) {}
