// Stub — the obstacle ticket implements spawning and collision.
export function updateObstacles(state, dt) {}

// Stub — the obstacle ticket implements rendering.
export function drawObstacles(ctx, state) {}
